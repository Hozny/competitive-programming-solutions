#include <bits/stdc++.h>

using namespace std;

int main() {
    int mm; 
    cin >> mm; 
    for (int ti = 0; ti < mm; ++ti) { 
        int N, K; 
        cin >> N >> K; 
        int a[N];
        for (int i = 0; i < N; ++i) { 
            cin >> a[i];
        }
  
    }
} 